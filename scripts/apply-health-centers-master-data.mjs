import fs from 'node:fs';

function read(path) { return fs.readFileSync(path, 'utf8'); }
function write(path, value) { fs.mkdirSync(path.split('/').slice(0, -1).join('/'), { recursive: true }); fs.writeFileSync(path, value); }
function replaceOnce(source, search, replacement, label) {
  if (!source.includes(search)) throw new Error(`Missing patch target: ${label}`);
  return source.replace(search, replacement);
}
function replaceRegex(source, regex, replacement, label) {
  if (!regex.test(source)) throw new Error(`Missing regex patch target: ${label}`);
  regex.lastIndex = 0;
  return source.replace(regex, replacement);
}

// Backend: health-center master data.
{
  const path = 'backend/health/models.py';
  let source = read(path);
  source = replaceOnce(source, `class HealthCenter(models.Model):\n    name = models.CharField(max_length=150, unique=True)\n    city = models.CharField(max_length=120, blank=True)\n    is_active = models.BooleanField(default=True)\n\n    def __str__(self):\n        return self.name\n`, `class HealthCenter(models.Model):\n    class BuildingType(models.TextChoices):\n        MODEL = 'model', 'Model'\n        RENTED = 'rented', 'Rented'\n        OWNED = 'owned', 'Owned'\n        OTHER = 'other', 'Other'\n\n    name = models.CharField(max_length=150, unique=True)\n    code = models.CharField(max_length=40, unique=True, null=True, blank=True)\n    region = models.CharField(max_length=120, blank=True, db_index=True)\n    city = models.CharField(max_length=120, blank=True, db_index=True)\n    district = models.CharField(max_length=120, blank=True, db_index=True)\n    building_type = models.CharField(max_length=20, choices=BuildingType.choices, default=BuildingType.MODEL, db_index=True)\n    is_active = models.BooleanField(default=True, db_index=True)\n    notes = models.TextField(blank=True)\n    created_at = models.DateTimeField(auto_now_add=True)\n    updated_at = models.DateTimeField(auto_now=True)\n\n    class Meta:\n        ordering = ['region', 'city', 'name']\n\n    def __str__(self):\n        return self.name\n`, 'HealthCenter model');
  write(path, source);
}

{
  const path = 'backend/health/serializers.py';
  let source = read(path);
  source = replaceOnce(source, `class HealthCenterSerializer(serializers.ModelSerializer):\n    class Meta: model = HealthCenter; fields = '__all__'\n`, `class HealthCenterSerializer(serializers.ModelSerializer):\n    employee_count = serializers.IntegerField(source='employees.count', read_only=True)\n    user_count = serializers.IntegerField(source='platform_users.count', read_only=True)\n    building_type_label = serializers.CharField(source='get_building_type_display', read_only=True)\n\n    class Meta:\n        model = HealthCenter\n        fields = '__all__'\n        read_only_fields = ('created_at', 'updated_at')\n\n    def validate_name(self, value):\n        value = str(value or '').strip()\n        if not value:\n            raise serializers.ValidationError('Health center name is required.')\n        queryset = HealthCenter.objects.filter(name__iexact=value)\n        if self.instance:\n            queryset = queryset.exclude(pk=self.instance.pk)\n        if queryset.exists():\n            raise serializers.ValidationError('A health center with this name already exists.')\n        return value\n\n    def validate_code(self, value):\n        value = str(value or '').strip().upper()\n        if not value:\n            return None\n        queryset = HealthCenter.objects.filter(code__iexact=value)\n        if self.instance:\n            queryset = queryset.exclude(pk=self.instance.pk)\n        if queryset.exists():\n            raise serializers.ValidationError('A health center with this code already exists.')\n        return value\n\n    def validate(self, attrs):\n        for field in ('region', 'city', 'district', 'notes'):\n            if field in attrs and attrs[field] is not None:\n                attrs[field] = str(attrs[field]).strip()\n        return attrs\n`, 'HealthCenter serializer');
  write(path, source);
}

{
  const path = 'backend/health/views.py';
  let source = read(path);
  source = replaceOnce(source, `class CanManageOccupationalHealthAssessments(permissions.BasePermission):`, `class CanManageHealthCenters(permissions.BasePermission):\n    def has_permission(self, request, view):\n        if not request.user or not request.user.is_authenticated:\n            return False\n        if request.method in permissions.SAFE_METHODS:\n            return True\n        if request.user.is_superuser or request.user.is_staff:\n            return True\n        try:\n            return request.user.health_profile.role == 'ohManager'\n        except Exception:\n            return False\n\n\nclass CanManageOccupationalHealthAssessments(permissions.BasePermission):`, 'health-center permission');
  source = replaceOnce(source, `class HealthCenterViewSet(viewsets.ModelViewSet):\n    queryset=HealthCenter.objects.all().order_by('name')\n    serializer_class=HealthCenterSerializer\n    permission_classes=[permissions.IsAuthenticated]\n`, `class HealthCenterViewSet(viewsets.ModelViewSet):\n    serializer_class = HealthCenterSerializer\n    permission_classes = [permissions.IsAuthenticated, CanManageHealthCenters]\n\n    def get_queryset(self):\n        queryset = HealthCenter.objects.all().order_by('region', 'city', 'name')\n        active = str(self.request.query_params.get('active') or '').lower()\n        if active in {'1', 'true', 'yes'}:\n            queryset = queryset.filter(is_active=True)\n        elif active in {'0', 'false', 'no'}:\n            queryset = queryset.filter(is_active=False)\n        region = str(self.request.query_params.get('region') or '').strip()\n        city = str(self.request.query_params.get('city') or '').strip()\n        building_type = str(self.request.query_params.get('building_type') or '').strip()\n        if region:\n            queryset = queryset.filter(region__iexact=region)\n        if city:\n            queryset = queryset.filter(city__iexact=city)\n        if building_type:\n            queryset = queryset.filter(building_type=building_type)\n        return queryset\n\n    def perform_create(self, serializer):\n        center = serializer.save()\n        AuditLog.objects.create(user=str(self.request.user), action='create_health_center', model_name='HealthCenter', record_id=str(center.id))\n\n    def perform_update(self, serializer):\n        center = serializer.save()\n        AuditLog.objects.create(user=str(self.request.user), action='update_health_center', model_name='HealthCenter', record_id=str(center.id))\n\n    def destroy(self, request, *args, **kwargs):\n        center = self.get_object()\n        center.is_active = False\n        center.save(update_fields=['is_active', 'updated_at'])\n        AuditLog.objects.create(user=str(request.user), action='deactivate_health_center', model_name='HealthCenter', record_id=str(center.id))\n        return Response({'status': 'deactivated', 'id': center.id, 'name': center.name}, status=status.HTTP_200_OK)\n`, 'HealthCenter viewset');
  write(path, source);
}

write('backend/health/migrations/0012_healthcenter_master_data.py', `from django.db import migrations, models\n\n\nclass Migration(migrations.Migration):\n    dependencies = [('health', '0011_expand_clinic_visit_history')]\n\n    operations = [\n        migrations.AddField(model_name='healthcenter', name='code', field=models.CharField(blank=True, max_length=40, null=True, unique=True)),\n        migrations.AddField(model_name='healthcenter', name='region', field=models.CharField(blank=True, db_index=True, max_length=120)),\n        migrations.AddField(model_name='healthcenter', name='district', field=models.CharField(blank=True, db_index=True, max_length=120)),\n        migrations.AddField(model_name='healthcenter', name='building_type', field=models.CharField(choices=[('model', 'Model'), ('rented', 'Rented'), ('owned', 'Owned'), ('other', 'Other')], db_index=True, default='model', max_length=20)),\n        migrations.AddField(model_name='healthcenter', name='notes', field=models.TextField(blank=True)),\n        migrations.AddField(model_name='healthcenter', name='created_at', field=models.DateTimeField(auto_now_add=True, null=True)),\n        migrations.AddField(model_name='healthcenter', name='updated_at', field=models.DateTimeField(auto_now=True, null=True)),\n        migrations.AlterField(model_name='healthcenter', name='city', field=models.CharField(blank=True, db_index=True, max_length=120)),\n        migrations.AlterField(model_name='healthcenter', name='is_active', field=models.BooleanField(db_index=True, default=True)),\n        migrations.AlterModelOptions(name='healthcenter', options={'ordering': ['region', 'city', 'name']}),\n    ]\n`);

// Backend: link periodic daily statistics to the health-center master record while retaining legacy location text.
{
  const path = 'backend/periodic_stats/models.py';
  let source = read(path);
  source = replaceOnce(source, `    location = models.CharField(max_length=255, blank=True)\n    executor = models.CharField(max_length=255, blank=True)`, `    health_center = models.ForeignKey('health.HealthCenter', on_delete=models.PROTECT, null=True, blank=True, related_name='daily_statistics')\n    location = models.CharField(max_length=255, blank=True)\n    executor = models.CharField(max_length=255, blank=True)`, 'DailyStatistic health center FK');
  write(path, source);
}

{
  const path = 'backend/periodic_stats/serializers.py';
  let source = read(path);
  source = replaceOnce(source, `    created_by_name = serializers.CharField(source='created_by.username', read_only=True)\n\n    class Meta:`, `    created_by_name = serializers.CharField(source='created_by.username', read_only=True)\n    health_center_name = serializers.CharField(source='health_center.name', read_only=True)\n\n    class Meta:`, 'DailyStatistic health-center name');
  write(path, source);
}

{
  const path = 'backend/periodic_stats/views.py';
  let source = read(path);
  source = replaceOnce(source, `queryset = DailyStatistic.objects.select_related('indicator', 'created_by').all()`, `queryset = DailyStatistic.objects.select_related('indicator', 'created_by', 'health_center').all()`, 'DailyStatistic select_related');
  write(path, source);
}

write('backend/periodic_stats/migrations/0005_daily_statistic_health_center.py', `from django.db import migrations, models\nimport django.db.models.deletion\n\n\ndef backfill_health_centers(apps, schema_editor):\n    DailyStatistic = apps.get_model('periodic_stats', 'DailyStatistic')\n    HealthCenter = apps.get_model('health', 'HealthCenter')\n    centers = {str(center.name).strip().casefold(): center.id for center in HealthCenter.objects.all()}\n    for row in DailyStatistic.objects.exclude(location='').iterator():\n        center_id = centers.get(str(row.location).strip().casefold())\n        if center_id:\n            row.health_center_id = center_id\n            row.save(update_fields=['health_center'])\n\n\nclass Migration(migrations.Migration):\n    dependencies = [\n        ('health', '0012_healthcenter_master_data'),\n        ('periodic_stats', '0004_evidenceattachment'),\n    ]\n\n    operations = [\n        migrations.AddField(\n            model_name='dailystatistic',\n            name='health_center',\n            field=models.ForeignKey(blank=True, null=True, on_delete=django.db.models.deletion.PROTECT, related_name='daily_statistics', to='health.healthcenter'),\n        ),\n        migrations.RunPython(backfill_health_centers, migrations.RunPython.noop),\n    ]\n`);

// Shared searchable health-center dropdown. Selected value displays center name only; option list carries location/building context.
write('frontend/src/app/components/HealthCenterAutocomplete.tsx', `import { Autocomplete, Box, Chip, TextField, Typography } from '@mui/material';\n\nexport type HealthCenterOption = {\n  id: number | string;\n  name: string;\n  code?: string | null;\n  region?: string;\n  city?: string;\n  district?: string;\n  building_type?: 'model' | 'rented' | 'owned' | 'other' | string;\n  building_type_label?: string;\n  is_active?: boolean;\n};\n\ntype Props = {\n  options: HealthCenterOption[];\n  value: string;\n  onChange: (value: string) => void;\n  label: string;\n  placeholder?: string;\n  required?: boolean;\n  disabled?: boolean;\n  allowEmpty?: boolean;\n};\n\nfunction buildingTypeLabel(type: string | undefined, isArabic: boolean) {\n  const labels: Record<string, [string, string]> = {\n    model: ['مبنى نموذجي', 'Model building'],\n    rented: ['مبنى مستأجر', 'Rented building'],\n    owned: ['مبنى مملوك', 'Owned building'],\n    other: ['أخرى', 'Other'],\n  };\n  const pair = labels[type || ''];\n  return pair ? pair[isArabic ? 0 : 1] : '';\n}\n\nexport function HealthCenterAutocomplete({ options, value, onChange, label, placeholder, required, disabled, allowEmpty = false }: Props) {\n  const selected = options.find(option => String(option.id) === String(value)) || null;\n  const isArabic = /[\\u0600-\\u06FF]/.test(label);\n  const activeOptions = options.filter(option => option.is_active !== false || String(option.id) === String(value));\n\n  return <Autocomplete\n    fullWidth\n    options={activeOptions}\n    value={selected}\n    disabled={disabled}\n    disableClearable={!allowEmpty}\n    isOptionEqualToValue={(option, current) => String(option.id) === String(current.id)}\n    getOptionLabel={option => option.name || ''}\n    onChange={(_, option) => onChange(option ? String(option.id) : '')}\n    renderOption={(props, option) => {\n      const meta = [option.region, option.city, option.district].filter(Boolean).join(' · ');\n      const building = buildingTypeLabel(option.building_type, isArabic);\n      return <Box component=\"li\" {...props} key={option.id} sx={{ display: 'block !important', py: 1.1 }}>\n        <Typography variant=\"body2\" fontWeight={850}>{option.name}</Typography>\n        {(meta || building) && <Box sx={{ display: 'flex', gap: .7, mt: .35, alignItems: 'center', flexWrap: 'wrap' }}>\n          {meta && <Typography variant=\"caption\" color=\"text.secondary\">{meta}</Typography>}\n          {building && <Chip size=\"small\" variant=\"outlined\" label={building} sx={{ height: 20 }} />}\n        </Box>}\n      </Box>;\n    }}\n    renderInput={params => <TextField {...params} required={required} label={label} placeholder={placeholder} />}\n  />;\n}\n`);

// Employee registry: use searchable master-data selector and active centers only.
{
  const path = 'frontend/src/app/pages/EmployeesPage.tsx';
  let source = read(path);
  source = replaceOnce(source, `import { CalendarDateField } from '../components/CalendarDateField';`, `import { CalendarDateField } from '../components/CalendarDateField';\nimport { HealthCenterAutocomplete } from '../components/HealthCenterAutocomplete';`, 'Employees autocomplete import');
  source = replaceOnce(source, `  city?: string;\n  is_active?: boolean;`, `  code?: string | null;\n  region?: string;\n  city?: string;\n  district?: string;\n  building_type?: string;\n  is_active?: boolean;`, 'Employee health-center type');
  source = source.replace(`apiRequest<unknown>('/health-centers/'),`, `apiRequest<unknown>('/health-centers/?active=true'),`);
  source = replaceOnce(source, `              <TextField fullWidth required select label={isRtl ? 'المركز الصحي' : 'Health Center'} value={form.health_center}\n                onChange={event => updateForm('health_center', event.target.value)} disabled={isReadOnly}>\n                {healthCenters.map(center => <MenuItem key={center.id} value={String(center.id)}>{center.name}</MenuItem>)}\n              </TextField>`, `              <HealthCenterAutocomplete\n                options={healthCenters}\n                value={form.health_center}\n                onChange={value => updateForm('health_center', value)}\n                label={isRtl ? 'المركز الصحي' : 'Health Center'}\n                placeholder={isRtl ? 'اختر أو ابحث باسم المركز' : 'Select or search by center name'}\n                required\n                disabled={isReadOnly}\n              />`, 'Employee health-center field');
  write(path, source);
}

// User administration: same master-data selector.
{
  const path = 'frontend/src/app/pages/AdminUsersPage.tsx';
  let source = read(path);
  source = replaceOnce(source, `import { mockHealthCenters } from '../data/mockData';`, `import { mockHealthCenters } from '../data/mockData';\nimport { HealthCenterAutocomplete } from '../components/HealthCenterAutocomplete';`, 'AdminUsers autocomplete import');
  source = replaceOnce(source, `interface HealthCenterOption {\n  id: string;\n  name: string;\n  nameAr?: string;\n}`, `interface HealthCenterOption {\n  id: string;\n  name: string;\n  nameAr?: string;\n  code?: string | null;\n  region?: string;\n  city?: string;\n  district?: string;\n  building_type?: string;\n  is_active?: boolean;\n}`, 'AdminUsers center interface');
  source = replaceOnce(source, `function normalizeHealthCenter(center: any): HealthCenterOption {\n  return { id: String(center.id), name: center.name || center.nameAr || '-', nameAr: center.nameAr || center.name };\n}`, `function normalizeHealthCenter(center: any): HealthCenterOption {\n  return {\n    id: String(center.id),\n    name: center.name || center.nameAr || '-',\n    nameAr: center.nameAr || center.name,\n    code: center.code || null,\n    region: center.region || '',\n    city: center.city || '',\n    district: center.district || '',\n    building_type: center.building_type || '',\n    is_active: center.is_active ?? true,\n  };\n}`, 'AdminUsers center normalization');
  source = source.replace(`apiRequest<any>('/health-centers/')`, `apiRequest<any>('/health-centers/?active=true')`);
  source = replaceOnce(source, `<Grid size={{ xs: 12, md: 4 }}><TextField fullWidth select label={isRtl ? 'المركز الصحي' : 'Health Center'} value={form.healthCenterId || ''} onChange={e => setForm(prev => ({ ...prev, healthCenterId: e.target.value }))}><MenuItem value="">-</MenuItem>{healthCenters.map(center => <MenuItem key={center.id} value={center.id}>{isRtl ? (center.nameAr || center.name) : center.name}</MenuItem>)}</TextField></Grid>`, `<Grid size={{ xs: 12, md: 4 }}><HealthCenterAutocomplete options={healthCenters} value={form.healthCenterId || ''} onChange={value => setForm(prev => ({ ...prev, healthCenterId: value }))} label={isRtl ? 'المركز الصحي' : 'Health Center'} placeholder={isRtl ? 'اختر أو ابحث باسم المركز' : 'Select or search center'} allowEmpty /></Grid>`, 'AdminUsers health-center field');
  write(path, source);
}

// Periodic statistics: replace free-text location/center with master-data health-center dropdown and persist FK.
{
  const path = 'frontend/src/app/pages/PeriodicStatisticsPage.tsx';
  let source = read(path);
  source = replaceOnce(source, `import { EvidenceAttachmentManager, type EvidenceAttachment } from '../components/EvidenceAttachmentManager';`, `import { EvidenceAttachmentManager, type EvidenceAttachment } from '../components/EvidenceAttachmentManager';\nimport { HealthCenterAutocomplete, type HealthCenterOption } from '../components/HealthCenterAutocomplete';`, 'Periodic autocomplete import');
  source = replaceOnce(source, `type DailyStatistic = { id: number; indicator: number; date: string; count: number; location: string; executor: string; notes: string };`, `type DailyStatistic = { id: number; indicator: number; date: string; count: number; health_center?: number | null; health_center_name?: string; location: string; executor: string; notes: string };`, 'Periodic DailyStatistic type');
  source = replaceOnce(source, `type DayMeta = { location: string; executor: string; notes: string };`, `type DayMeta = { health_center: string; executor: string; notes: string };`, 'Periodic DayMeta type');
  source = replaceOnce(source, `  const [documents, setDocuments] = useState<ReferenceDocument[]>([]);`, `  const [documents, setDocuments] = useState<ReferenceDocument[]>([]);\n  const [healthCenters, setHealthCenters] = useState<HealthCenterOption[]>([]);`, 'Periodic center state');
  source = replaceOnce(source, `      fetchCollection<ReferenceDocument>('/periodic-statistics/reference-documents/'),\n      fetchCollection<DailyStatistic>(\`/periodic-statistics/daily-statistics/?year=\${year}&month=\${month}\`),\n    ]).then(([summaryPayload, workforceRows, documentRows, dailyRows]) => {`, `      fetchCollection<ReferenceDocument>('/periodic-statistics/reference-documents/'),\n      fetchCollection<HealthCenterOption>('/health-centers/?active=true'),\n      fetchCollection<DailyStatistic>(\`/periodic-statistics/daily-statistics/?year=\${year}&month=\${month}\`),\n    ]).then(([summaryPayload, workforceRows, documentRows, centerRows, dailyRows]) => {`, 'Periodic load centers');
  source = replaceOnce(source, `      setDocuments(documentRows);\n      setMonthEntries(dailyRows);`, `      setDocuments(documentRows);\n      setHealthCenters(centerRows);\n      setMonthEntries(dailyRows);`, 'Periodic set centers');
  source = replaceOnce(source, `        if (!meta[day]) meta[day] = { location: row.location || '', executor: row.executor || '', notes: row.notes || '' };`, `        if (!meta[day]) {\n          const matchedCenter = row.health_center || centerRows.find(center => center.name === row.location)?.id || '';\n          meta[day] = { health_center: String(matchedCenter || ''), executor: row.executor || '', notes: row.notes || '' };\n        }`, 'Periodic metadata mapping');
  source = replaceOnce(source, `    setDayMeta(current => ({ ...current, [day]: { location: '', executor: '', notes: '', ...(current[day] || {}), [field]: value } }));`, `    setDayMeta(current => ({ ...current, [day]: { health_center: '', executor: '', notes: '', ...(current[day] || {}), [field]: value } }));`, 'Periodic updateDayMeta');
  source = replaceOnce(source, `        const meta = dayMeta[day] || { location: '', executor: '', notes: '' };`, `        const meta = dayMeta[day] || { health_center: '', executor: '', notes: '' };`, 'Periodic save meta default');
  source = replaceOnce(source, `            const payload = { indicator: indicator.id, date, count, location: meta.location, executor: meta.executor, notes: meta.notes };`, `            const selectedCenter = healthCenters.find(center => String(center.id) === String(meta.health_center));\n            const payload = { indicator: indicator.id, date, count, health_center: meta.health_center ? Number(meta.health_center) : null, location: selectedCenter?.name || '', executor: meta.executor, notes: meta.notes };`, 'Periodic save center payload');
  source = source.replace(`          <TableCell>{isRtl ? 'الموقع / المركز' : 'Location'}</TableCell>`, `          <TableCell sx={{ minWidth: 230 }}>{isRtl ? 'المركز الصحي' : 'Health Center'}</TableCell>`);
  source = replaceOnce(source, `            const meta = dayMeta[day] || { location: '', executor: '', notes: '' };`, `            const meta = dayMeta[day] || { health_center: '', executor: '', notes: '' };`, 'Periodic row meta default');
  source = replaceOnce(source, `              <TableCell><TextField size="small" value={meta.location} disabled={!canEdit} onChange={event => updateDayMeta(day, 'location', event.target.value)} /></TableCell>`, `              <TableCell><HealthCenterAutocomplete options={healthCenters} value={meta.health_center} onChange={value => updateDayMeta(day, 'health_center', value)} label={isRtl ? 'المركز' : 'Center'} disabled={!canEdit} allowEmpty /></TableCell>`, 'Periodic center cell');
  source = source.replace(`'أدخل العدد في تقاطع اليوم مع المؤشر، ويمكن إضافة الموقع والمنفذ والملاحظات لكل يوم.'`, `'أدخل العدد في تقاطع اليوم مع المؤشر، واختر المركز الصحي من قاعدة المراكز المعتمدة لكل يوم.'`);
  write(path, source);
}

// Health-centers administration page.
write('frontend/src/app/pages/HealthCentersPage.tsx', `import { useEffect, useMemo, useState } from 'react';\nimport { useTranslation } from 'react-i18next';\nimport {\n  Alert, Box, Button, Chip, CircularProgress, Dialog, DialogActions, DialogContent, DialogTitle,\n  InputAdornment, MenuItem, Paper, Stack, Switch, Table, TableBody, TableCell, TableContainer,\n  TableHead, TableRow, TextField, Typography,\n} from '@mui/material';\nimport {\n  Add as AddIcon, Business as BusinessIcon, Edit as EditIcon, Refresh as RefreshIcon,\n  Search as SearchIcon, ToggleOff as ToggleOffIcon,\n} from '@mui/icons-material';\nimport { toast } from 'sonner';\nimport { authFetch, getAccessToken } from '../context/AuthContext';\n\nconst PRODUCTION_API_BASE_URL = 'https://occupational-health-platform-production.up.railway.app/api';\nconst API_BASE_URL = (import.meta.env.VITE_API_URL || PRODUCTION_API_BASE_URL).replace(/\\/$/, '');\n\ntype Center = {\n  id: number; name: string; code?: string | null; region: string; city: string; district: string;\n  building_type: 'model' | 'rented' | 'owned' | 'other'; building_type_label?: string;\n  is_active: boolean; notes?: string; employee_count?: number; user_count?: number;\n};\ntype CenterForm = Omit<Center, 'id' | 'building_type_label' | 'employee_count' | 'user_count'>;\nconst EMPTY_FORM: CenterForm = { name: '', code: '', region: '', city: '', district: '', building_type: 'model', is_active: true, notes: '' };\n\nfunction list<T>(payload: any): T[] { return Array.isArray(payload) ? payload : Array.isArray(payload?.results) ? payload.results : []; }\nasync function request<T>(path: string, init: RequestInit = {}) {\n  const token = getAccessToken();\n  if (!token) throw new Error('Authentication required');\n  const response = await authFetch(\`${API_BASE_URL}\${path}\`, { ...init, headers: { Authorization: \`Bearer \${token}\`, ...(init.body ? { 'Content-Type': 'application/json' } : {}), ...(init.headers || {}) } });\n  const body = await response.json().catch(() => null);\n  if (!response.ok) {\n    const first = body && typeof body === 'object' ? Object.values(body)[0] : null;\n    throw new Error(typeof body?.detail === 'string' ? body.detail : Array.isArray(first) ? String(first[0]) : String(first || 'Request failed'));\n  }\n  return body as T;\n}\n\nexport function HealthCentersPage() {\n  const { i18n } = useTranslation();\n  const isRtl = i18n.language === 'ar';\n  const [centers, setCenters] = useState<Center[]>([]);\n  const [loading, setLoading] = useState(true);\n  const [saving, setSaving] = useState(false);\n  const [search, setSearch] = useState('');\n  const [regionFilter, setRegionFilter] = useState('all');\n  const [cityFilter, setCityFilter] = useState('all');\n  const [statusFilter, setStatusFilter] = useState<'all' | 'active' | 'inactive'>('all');\n  const [open, setOpen] = useState(false);\n  const [editing, setEditing] = useState<Center | null>(null);\n  const [form, setForm] = useState<CenterForm>(EMPTY_FORM);\n\n  async function load() {\n    setLoading(true);\n    try { setCenters(list<Center>(await request('/health-centers/'))); }\n    catch (error) { toast.error(error instanceof Error ? error.message : 'Failed to load health centers'); }\n    finally { setLoading(false); }\n  }\n  useEffect(() => { void load(); }, []);\n\n  const regions = useMemo(() => [...new Set(centers.map(c => c.region).filter(Boolean))].sort(), [centers]);\n  const cities = useMemo(() => [...new Set(centers.filter(c => regionFilter === 'all' || c.region === regionFilter).map(c => c.city).filter(Boolean))].sort(), [centers, regionFilter]);\n  const filtered = useMemo(() => centers.filter(center => {\n    const q = search.trim().toLowerCase();\n    const matchesSearch = !q || [center.name, center.code, center.region, center.city, center.district].filter(Boolean).join(' ').toLowerCase().includes(q);\n    const matchesRegion = regionFilter === 'all' || center.region === regionFilter;\n    const matchesCity = cityFilter === 'all' || center.city === cityFilter;\n    const matchesStatus = statusFilter === 'all' || (statusFilter === 'active' ? center.is_active : !center.is_active);\n    return matchesSearch && matchesRegion && matchesCity && matchesStatus;\n  }), [centers, search, regionFilter, cityFilter, statusFilter]);\n\n  const buildingLabel = (type: Center['building_type']) => ({ model: isRtl ? 'نموذجي' : 'Model', rented: isRtl ? 'مستأجر' : 'Rented', owned: isRtl ? 'مملوك' : 'Owned', other: isRtl ? 'أخرى' : 'Other' }[type]);\n  function openAdd() { setEditing(null); setForm(EMPTY_FORM); setOpen(true); }\n  function openEdit(center: Center) { setEditing(center); setForm({ name: center.name, code: center.code || '', region: center.region || '', city: center.city || '', district: center.district || '', building_type: center.building_type || 'model', is_active: center.is_active, notes: center.notes || '' }); setOpen(true); }\n  function update<K extends keyof CenterForm>(key: K, value: CenterForm[K]) { setForm(current => ({ ...current, [key]: value })); }\n\n  async function save() {\n    if (!form.name.trim()) return toast.error(isRtl ? 'اسم المركز الصحي مطلوب' : 'Health center name is required');\n    setSaving(true);\n    try {\n      const payload = { ...form, name: form.name.trim(), code: form.code?.trim() || null, region: form.region.trim(), city: form.city.trim(), district: form.district.trim(), notes: form.notes?.trim() || '' };\n      if (editing) await request(\`/health-centers/\${editing.id}/\`, { method: 'PATCH', body: JSON.stringify(payload) });\n      else await request('/health-centers/', { method: 'POST', body: JSON.stringify(payload) });\n      toast.success(isRtl ? 'تم حفظ بيانات المركز الصحي' : 'Health center saved');\n      setOpen(false); await load();\n    } catch (error) { toast.error(error instanceof Error ? error.message : 'Save failed'); }\n    finally { setSaving(false); }\n  }\n\n  async function toggleCenter(center: Center) {\n    try {\n      await request(\`/health-centers/\${center.id}/\`, { method: 'PATCH', body: JSON.stringify({ is_active: !center.is_active }) });\n      toast.success(center.is_active ? (isRtl ? 'تم تعطيل المركز مع الاحتفاظ بالسجلات السابقة' : 'Center deactivated; historical records kept') : (isRtl ? 'تم تفعيل المركز' : 'Center activated'));\n      await load();\n    } catch (error) { toast.error(error instanceof Error ? error.message : 'Update failed'); }\n  }\n\n  return <Box dir={isRtl ? 'rtl' : 'ltr'}>\n    <Stack direction={{ xs: 'column', md: 'row' }} justifyContent=\"space-between\" alignItems={{ xs: 'stretch', md: 'center' }} spacing={2} sx={{ mb: 3 }}>\n      <Stack direction=\"row\" spacing={1.5} alignItems=\"center\"><BusinessIcon sx={{ fontSize: 38, color: 'primary.main' }} /><Box><Typography variant=\"h4\" fontWeight={950}>{isRtl ? 'إدارة المراكز الصحية' : 'Health Centers Management'}</Typography><Typography variant=\"body2\" color=\"text.secondary\">{isRtl ? 'قاعدة البيانات المركزية المعتمدة لأسماء المراكز ومواقعها ونوع المبنى' : 'Central master data for health centers, locations, and building type'}</Typography></Box></Stack>\n      <Stack direction=\"row\" spacing={1}><Button variant=\"outlined\" startIcon={<RefreshIcon />} onClick={() => void load()}>{isRtl ? 'تحديث' : 'Refresh'}</Button><Button variant=\"contained\" startIcon={<AddIcon />} onClick={openAdd}>{isRtl ? 'إضافة مركز صحي' : 'Add Health Center'}</Button></Stack>\n    </Stack>\n\n    <Box sx={{ display: 'grid', gridTemplateColumns: { xs: '1fr 1fr', md: 'repeat(4,1fr)' }, gap: 2, mb: 2.5 }}>\n      {[\n        [isRtl ? 'إجمالي المراكز' : 'Total centers', centers.length],\n        [isRtl ? 'المراكز النشطة' : 'Active centers', centers.filter(c => c.is_active).length],\n        [isRtl ? 'المباني النموذجية' : 'Model buildings', centers.filter(c => c.building_type === 'model').length],\n        [isRtl ? 'المباني المستأجرة' : 'Rented buildings', centers.filter(c => c.building_type === 'rented').length],\n      ].map(([label, value]) => <Paper key={String(label)} sx={{ p: 2, textAlign: 'center' }}><Typography variant=\"h4\" fontWeight={950} color=\"primary.main\">{value}</Typography><Typography variant=\"body2\" color=\"text.secondary\">{label}</Typography></Paper>)}\n    </Box>\n\n    <Paper sx={{ p: 2, mb: 2.5 }}><Box sx={{ display: 'grid', gridTemplateColumns: { xs: '1fr', md: '2fr 1fr 1fr 1fr' }, gap: 1.5 }}>\n      <TextField value={search} onChange={e => setSearch(e.target.value)} placeholder={isRtl ? 'بحث باسم المركز أو الكود أو الموقع...' : 'Search center, code, or location...'} slotProps={{ input: { startAdornment: <InputAdornment position=\"start\"><SearchIcon /></InputAdornment> } }} />\n      <TextField select label={isRtl ? 'المنطقة' : 'Region'} value={regionFilter} onChange={e => { setRegionFilter(e.target.value); setCityFilter('all'); }}><MenuItem value=\"all\">{isRtl ? 'كل المناطق' : 'All regions'}</MenuItem>{regions.map(item => <MenuItem key={item} value={item}>{item}</MenuItem>)}</TextField>\n      <TextField select label={isRtl ? 'المدينة' : 'City'} value={cityFilter} onChange={e => setCityFilter(e.target.value)}><MenuItem value=\"all\">{isRtl ? 'كل المدن' : 'All cities'}</MenuItem>{cities.map(item => <MenuItem key={item} value={item}>{item}</MenuItem>)}</TextField>\n      <TextField select label={isRtl ? 'الحالة' : 'Status'} value={statusFilter} onChange={e => setStatusFilter(e.target.value as typeof statusFilter)}><MenuItem value=\"all\">{isRtl ? 'الكل' : 'All'}</MenuItem><MenuItem value=\"active\">{isRtl ? 'نشط' : 'Active'}</MenuItem><MenuItem value=\"inactive\">{isRtl ? 'غير نشط' : 'Inactive'}</MenuItem></TextField>\n    </Box></Paper>\n\n    {loading ? <Paper sx={{ p: 6, textAlign: 'center' }}><CircularProgress /></Paper> : <TableContainer component={Paper}><Table><TableHead><TableRow>\n      <TableCell>{isRtl ? 'اسم المركز الصحي' : 'Health Center'}</TableCell><TableCell>{isRtl ? 'الكود' : 'Code'}</TableCell><TableCell>{isRtl ? 'المنطقة' : 'Region'}</TableCell><TableCell>{isRtl ? 'المدينة' : 'City'}</TableCell><TableCell>{isRtl ? 'الحي' : 'District'}</TableCell><TableCell>{isRtl ? 'نوع المبنى' : 'Building'}</TableCell><TableCell>{isRtl ? 'الحالة' : 'Status'}</TableCell><TableCell align=\"center\">{isRtl ? 'الإجراءات' : 'Actions'}</TableCell>\n    </TableRow></TableHead><TableBody>{filtered.map(center => <TableRow key={center.id} hover>\n      <TableCell><Typography fontWeight={900}>{center.name}</Typography>{(center.employee_count || center.user_count) ? <Typography variant=\"caption\" color=\"text.secondary\">{isRtl ? 'مرتبط' : 'Linked'}: {center.employee_count || 0} / {center.user_count || 0}</Typography> : null}</TableCell><TableCell>{center.code || '—'}</TableCell><TableCell>{center.region || '—'}</TableCell><TableCell>{center.city || '—'}</TableCell><TableCell>{center.district || '—'}</TableCell><TableCell><Chip size=\"small\" label={buildingLabel(center.building_type)} /></TableCell><TableCell><Chip size=\"small\" color={center.is_active ? 'success' : 'default'} label={center.is_active ? (isRtl ? 'نشط' : 'Active') : (isRtl ? 'غير نشط' : 'Inactive')} /></TableCell><TableCell align=\"center\"><Button size=\"small\" startIcon={<EditIcon />} onClick={() => openEdit(center)}>{isRtl ? 'تعديل' : 'Edit'}</Button><Button size=\"small\" color={center.is_active ? 'warning' : 'success'} startIcon={<ToggleOffIcon />} onClick={() => void toggleCenter(center)}>{center.is_active ? (isRtl ? 'تعطيل' : 'Deactivate') : (isRtl ? 'تفعيل' : 'Activate')}</Button></TableCell>\n    </TableRow>)}{!filtered.length && <TableRow><TableCell colSpan={8}><Alert severity=\"info\">{isRtl ? 'لا توجد مراكز مطابقة.' : 'No matching health centers.'}</Alert></TableCell></TableRow>}</TableBody></Table></TableContainer>}\n\n    <Dialog open={open} onClose={() => !saving && setOpen(false)} maxWidth=\"md\" fullWidth><DialogTitle>{editing ? (isRtl ? 'تعديل المركز الصحي' : 'Edit Health Center') : (isRtl ? 'إضافة مركز صحي' : 'Add Health Center')}</DialogTitle><DialogContent dividers><Box sx={{ display: 'grid', gridTemplateColumns: { xs: '1fr', md: 'repeat(2,1fr)' }, gap: 2 }}>\n      <TextField required label={isRtl ? 'اسم المركز الصحي' : 'Health Center Name'} value={form.name} onChange={e => update('name', e.target.value)} />\n      <TextField label={isRtl ? 'كود المركز' : 'Center Code'} value={form.code || ''} onChange={e => update('code', e.target.value.toUpperCase())} helperText={isRtl ? 'اختياري، ويجب أن يكون فريدًا' : 'Optional and unique'} />\n      <TextField label={isRtl ? 'المنطقة' : 'Region'} value={form.region} onChange={e => update('region', e.target.value)} />\n      <TextField label={isRtl ? 'المدينة' : 'City'} value={form.city} onChange={e => update('city', e.target.value)} />\n      <TextField label={isRtl ? 'الحي' : 'District'} value={form.district} onChange={e => update('district', e.target.value)} />\n      <TextField select label={isRtl ? 'نوع المبنى' : 'Building Type'} value={form.building_type} onChange={e => update('building_type', e.target.value as CenterForm['building_type'])}><MenuItem value=\"model\">{isRtl ? 'مبنى نموذجي' : 'Model building'}</MenuItem><MenuItem value=\"rented\">{isRtl ? 'مبنى مستأجر' : 'Rented building'}</MenuItem><MenuItem value=\"owned\">{isRtl ? 'مبنى مملوك' : 'Owned building'}</MenuItem><MenuItem value=\"other\">{isRtl ? 'أخرى' : 'Other'}</MenuItem></TextField>\n      <TextField multiline minRows={3} label={isRtl ? 'ملاحظات' : 'Notes'} value={form.notes || ''} onChange={e => update('notes', e.target.value)} sx={{ gridColumn: { md: '1 / -1' } }} />\n      <Stack direction=\"row\" alignItems=\"center\" spacing={1}><Switch checked={form.is_active} onChange={e => update('is_active', e.target.checked)} /><Typography>{isRtl ? 'المركز نشط ومتاح في القوائم المنسدلة' : 'Center is active and available in dropdowns'}</Typography></Stack>\n    </Box></DialogContent><DialogActions><Button onClick={() => setOpen(false)} disabled={saving}>{isRtl ? 'إلغاء' : 'Cancel'}</Button><Button variant=\"contained\" onClick={() => void save()} disabled={saving}>{saving ? (isRtl ? 'جاري الحفظ...' : 'Saving...') : (isRtl ? 'حفظ' : 'Save')}</Button></DialogActions></Dialog>\n  </Box>;\n}\n`);

// Route + sidebar.
{
  const path = 'frontend/src/app/App.tsx';
  let source = read(path);
  source = replaceOnce(source, `import { PeriodicStatisticsPage } from './pages/PeriodicStatisticsPage';`, `import { PeriodicStatisticsPage } from './pages/PeriodicStatisticsPage';\nimport { HealthCentersPage } from './pages/HealthCentersPage';`, 'App health centers import');
  source = replaceOnce(source, `        <Route path="admin/users" element={\n          <PermissionRoute permission="manage:users">\n            <AdminUsersPage />\n          </PermissionRoute>\n        } />`, `        <Route path="admin/users" element={\n          <PermissionRoute permission="manage:users">\n            <AdminUsersPage />\n          </PermissionRoute>\n        } />\n        <Route path="admin/health-centers" element={\n          <PermissionRoute permission="manage:users">\n            <HealthCentersPage />\n          </PermissionRoute>\n        } />`, 'App health centers route');
  write(path, source);
}

{
  const path = 'frontend/src/app/components/layout/DashboardLayout.tsx';
  let source = read(path);
  source = replaceOnce(source, `  BadgeOutlined as HealthCardIcon,\n} from '@mui/icons-material';`, `  BadgeOutlined as HealthCardIcon,\n  Business as BusinessIcon,\n} from '@mui/icons-material';`, 'Sidebar business icon');
  source = replaceOnce(source, `    { key: 'adminUsers',         label: isRtl ? 'إدارة المستخدمين' : 'Users Management',       icon: <PeopleIcon />,        path: '/admin/users', group: 'admin', permission: PERMISSIONS.MANAGE_USERS },`, `    { key: 'adminUsers',         label: isRtl ? 'إدارة المستخدمين' : 'Users Management',       icon: <PeopleIcon />,        path: '/admin/users', group: 'admin', permission: PERMISSIONS.MANAGE_USERS },\n    { key: 'healthCenters',      label: isRtl ? 'إدارة المراكز الصحية' : 'Health Centers',      icon: <BusinessIcon />,      path: '/admin/health-centers', group: 'admin', permission: PERMISSIONS.MANAGE_USERS },`, 'Sidebar center item');
  write(path, source);
}

console.log('Health-center master-data implementation applied.');
