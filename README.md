# Occupational Health Platform

منصة إدارة الصحة المهنية للموظفين.

This repository contains a monorepo with:

- `frontend`: React + Vite + TypeScript + Tailwind CSS
- `backend`: Django + Django REST Framework + PostgreSQL, ready for Railway deployment
